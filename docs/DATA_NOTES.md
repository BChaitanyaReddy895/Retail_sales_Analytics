# Data Notes

The project uses these raw tables:

- `regions.csv`
- `customers.csv`
- `products.csv`
- `inventory.csv`
- `sales.csv`

The ETL pipeline reads the raw files from `data/raw/`, processes them, and loads the cleaned tables into the database.
